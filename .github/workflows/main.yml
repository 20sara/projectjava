
on:
  push:
    branches:
      - docker


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "login to Docker Hup"
        run: docker login --username Sara2Amin --password dckr_pat_pby9DR2-FS1LPjOZuwxst2RN5rQ

       